{
  "name": "violettunes",
  "version": "1.0.0",
  "description": "VioletTunes - Self-hosted music platform",
  "scripts": {
    "start": "cd backend && node server.js",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
